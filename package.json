{
  "name": "web3-interactive-demo",
  "version": "1.0.0",
  "description": "A Web3 interactive demo with drag-and-drop widgets, crypto payments, and social media integration",
  "main": "index.html",
  "scripts": {
    "dev": "live-server --port=3000 --host=localhost",
    "build": "echo 'Static site - no build needed'",
    "copy-files": "echo 'Files ready for deployment'",
    "deploy": "npm run build",
    "test": "echo \"No tests specified\" && exit 0"
  },
  "keywords": [
    "web3",
    "crypto",
    "social-media",
    "drag-and-drop",
    "metamask",
    "twitter-api",
    "instagram-api",
    "youtube-api"
  ],
  "author": "Your Name",
  "license": "MIT",
  "devDependencies": {
    "live-server": "^1.2.2"
  },
  "dependencies": {
    "ethers": "^6.8.0"
  },
  "engines": {
    "node": ">=16.0.0"
  }
}